
      <nav class="navbar container navbar-dark bg-inverse header-nav">
      <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#exCollapsingNavbar2">
        &#9776;
      </button>
      <div class="collapse navbar-toggleable-xs" id="exCollapsingNavbar2">
          <a class="navbar-brand" href="/"><img src="<%= asset_path 'archipelago-logo_w.png' %>" alt="" width="150"></a>
        <ul class="nav navbar-nav">
          <li class="nav-item <%= 'active' if current_page?(root_path) %>">
              <a class="nav-link" href="/">Home</a>
          </li>

          <li class="nav-item <%= 'active' if current_page?(about_path) %>">
            <a class="nav-link" href="/about">About</a>
          </li>
          <li class="nav-item <%= 'active' if current_page?(contact_path) %>">
            <a class="nav-link" href="/contact">Contact</a>
          </li>
          <li class="nav-item <%= 'active' if current_page?(groups_path) %>">
              <%= link_to 'Groups', groups_path, class: 'nav-link' %>
          </li>


          <% if user_signed_in? %>
          <li class="nav-item <%= 'active' if current_page?(messages_path) %>">
               <a href="/messages" class="nav-link">Inbox
                   <% unless (@mymsgs <= 0) %>
                   <span class="label label-danger"><%= @mymsgs %></span>
                   <% end %>
                </a>
          </li>
           <% if current_user.has_role? :admin %>
            <li class="nav-item <%= 'active' if current_page?(admin_path) %>"><%= link_to 'Admin', admin_path, class: 'nav-link' %></li>
            <% end %>

            <% if current_user.profile %>
            <li class="nav-item pull-xs-right">
               <a href="<%= profile_path(current_user.profile) %>"><img src="<%= current_user.profile.profile_pic %>" alt="" width="50" style="display: block;" class="img-rounded"></a>
            </li>
            <li class="nav-item pull-xs-right hidden-md-down">
                <%= link_to current_user.email , profile_path(current_user.profile), class: 'nav-link' %>
            </li>
            <% else %>
            <li class="nav-item pull-xs-right">
                <%= link_to 'Set up your profile', new_profile_path, class: 'nav-link' %>
            </li>
            <% end %>

          <li class="nav-item pull-xs-right">
              <%= link_to 'Sign Out', destroy_user_session_path, method: :delete, class: 'nav-link' %>
          </li>
            <% else %>
            <li class="nav-item pull-xs-right">
                <%= link_to 'Sign In', new_user_session_path, class: 'nav-link' %>
            </li>
            <% end %>

        </ul>
          </div>
    </nav>
    <div class="container">
       <p class="notice" ><%= notice %></p>
        <p class="alert"><%= alert %></p>
        </div>

